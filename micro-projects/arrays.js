//An empty array
var numArray = [];

//index of arrays
var i = 0;
var values = 0;

//the length of array to push to empty array
numArray.length = 20;
numArray.push(values);


//While loop to iterate as it pushes
while(i < numArray.length){
    values += i;
    console.log(values);
    i++
}

